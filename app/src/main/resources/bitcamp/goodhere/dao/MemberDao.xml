<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bitcamp.goodhere.dao.MemberDao">

  <resultMap type="member" id="memberMap">
    <id column="member_id" property="no"/>
    <result column="email" property="email"/>
    <result column="name" property="name"/>
    <result column="tel" property="tel"/>
    <result column="nickname" property="nickname"/>
    <result column="intorduce" property="intorduce"/>
    <result column="photo" property="photo"/>
    <result column="created_date" property="created_date"/>
    <result column="state" property="state"/>
    <result column="auth" property="auth"/>
  </resultMap>

  <insert id="insert" parameterType="member" 
          useGeneratedKeys="true" keyProperty="no" keyColumn="member_id">
    insert into gh_member(email, pwd, name, tel, nickname)
    values(#{email}, sha2(#{password},256), #{name}, #{tel}, #{nickname}, #{introduce}, #{photo}, #{state}, #{auth})
  </insert>

  <select id="findAll" resultMap="memberMap">
    select 
      member_id, 
      name, 
      email, 
      created_date 
    from 
      gh_member
    order by 
      member_id desc
  </select>

  <select id="findByNo" parameterType="int" resultMap="memberMap">
    select 
      member_id, 
      name, 
      email, 
      tel,
      created_date 
    from 
      gh_member
    where 
      member_id=#{no}
  </select>
  
  <update id="update" parameterType="member">
    update gh_member set
      name=#{name}, 
      email=#{email}, 
      pwd=sha2(#{password},256), 
      tel=#{tel}
    where member_id=#{no}
  </update>
  
  <delete id="delete" parameterType="int">
    delete from gh_member
    where member_id=#{no}
  </delete>
</mapper>





